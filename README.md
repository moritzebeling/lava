# Lava animation

## Dependencies
- `simplex-noise`

## Usage

```js
import { Lava } from './script.js';
let animation = new Lava( element );
```

Options
```js
let animation = new Lava( element, {
    color: {
        r: 0, g: 0, b: 0
    }
});
```